<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aiready.dict.mapper.DictTypeMapper">

    <resultMap id="BaseResultMap" type="com.aiready.dict.entity.DictType">
        <id column="id" property="id"/>
        <result column="dict_type_code" property="dictTypeCode"/>
        <result column="dict_type_name" property="dictTypeName"/>
        <result column="description" property="description"/>
        <result column="status" property="status"/>
        <result column="sort_order" property="sortOrder"/>
        <result column="remark" property="remark"/>
        <result column="create_by" property="createBy"/>
        <result column="create_time" property="createTime"/>
        <result column="update_by" property="updateBy"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, dict_type_code, dict_type_name, description, status, sort_order, remark,
        create_by, create_time, update_by, update_time, deleted
    </sql>

    <!-- 检查字典类型编码是否存在 -->
    <select id="checkDictTypeCodeExists" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM sys_dict_type
        WHERE dict_type_code = #{dictTypeCode}
          AND deleted = 0
          <if test="excludeId != null">
              AND id != #{excludeId}
          </if>
    </select>

    <!-- 获取最大排序号 -->
    <select id="selectMaxSortOrder" resultType="java.lang.Integer">
        SELECT MAX(sort_order)
        FROM sys_dict_type
        WHERE deleted = 0
    </select>

</mapper>