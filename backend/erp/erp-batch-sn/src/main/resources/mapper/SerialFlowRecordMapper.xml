<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.aiedge.erp.batchsn.mapper.SerialFlowRecordMapper">

    <select id="selectBySerialId" resultType="cn.aiedge.erp.batchsn.entity.SerialFlowRecord">
        SELECT * FROM serial_flow_record
        WHERE serial_id = #{serialId}
        ORDER BY created_at DESC
    </select>

    <select id="selectFullHistory" resultType="cn.aiedge.erp.batchsn.entity.SerialFlowRecord">
        SELECT * FROM serial_flow_record
        WHERE serial_no = #{serialNo}
        ORDER BY created_at DESC
    </select>

    <select id="selectStatusChangeHistory" resultType="cn.aiedge.erp.batchsn.entity.SerialFlowRecord">
        SELECT * FROM serial_flow_record
        WHERE serial_id = #{serialId}
        <if test="status != null and status != ''">
            AND (from_status = #{status} OR to_status = #{status})
        </if>
        ORDER BY created_at DESC
    </select>

    <select id="selectMaintenanceRecords" resultType="cn.aiedge.erp.batchsn.entity.SerialFlowRecord">
        SELECT * FROM serial_flow_record
        WHERE serial_id = #{serialId} AND flow_type = 'MAINTAIN'
        ORDER BY created_at DESC
    </select>

</mapper>
