# 核心依赖
httpx>=0.27,<1.0
SQLAlchemy>=2.0,<3.0
asyncpg>=0.29,<1.0
psycopg2-binary>=2.9,<3.0     # 同步连接（建表等）
pydantic>=2.0,<3.0
pydantic-settings>=2.0,<3.0

# 调度
APScheduler>=3.10,<4.0

# 日志
loguru>=0.7,<1.0

# HTTP API（供 Java 后端调用）
aiohttp>=3.9,<4.0

# 开发辅助
python-dotenv>=1.0,<2.0
