# 简单Dockerfile - 使用已构建的jar文件
FROM openjdk:17-alpine

WORKDIR /app

# 复制已构建的jar文件
COPY target/core-api-1.0.0-SNAPSHOT.jar app.jar

# 创建非root用户
RUN addgroup -S ai-ready && adduser -S ai-ready -G ai-ready
USER ai-ready

# 暴露端口
EXPOSE 8080

# 启动命令
ENTRYPOINT ["java", "-jar", "/app/app.jar"]