<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.aiedge.base.mapper.UserMapper">

    <!-- 根据用户名查询用户 -->
    <select id="selectByUsername" resultType="cn.aiedge.base.entity.User">
        SELECT * FROM sys_user WHERE username = #{username} AND deleted = 0
    </select>

    <!-- 根据手机号查询用户 -->
    <select id="selectByPhone" resultType="cn.aiedge.base.entity.User">
        SELECT * FROM sys_user WHERE phone = #{phone} AND deleted = 0
    </select>

    <!-- 根据邮箱查询用户 -->
    <select id="selectByEmail" resultType="cn.aiedge.base.entity.User">
        SELECT * FROM sys_user WHERE email = #{email} AND deleted = 0
    </select>

    <!-- 根据角色ID查询用户列表 -->
    <select id="selectByRoleId" resultType="cn.aiedge.base.entity.User">
        SELECT u.* FROM sys_user u
        INNER JOIN sys_user_role ur ON u.id = ur.user_id
        WHERE ur.role_id = #{roleId} AND u.deleted = 0
        ORDER BY u.create_time DESC
    </select>

    <!-- 根据部门ID查询用户列表 -->
    <select id="selectByDeptId" resultType="cn.aiedge.base.entity.User">
        SELECT * FROM sys_user WHERE dept_id = #{deptId} AND deleted = 0
        ORDER BY create_time DESC
    </select>

    <!-- 更新用户最后登录信息 -->
    <update id="updateLoginInfo">
        UPDATE sys_user 
        SET last_login_time = NOW(),
            last_login_ip = #{loginIp},
            update_time = NOW()
        WHERE id = #{id}
    </update>

</mapper>
