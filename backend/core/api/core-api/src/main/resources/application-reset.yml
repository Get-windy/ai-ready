# 重置配置 - 用于首次设置或密码重置
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/devdb?useUnicode=true&characterEncoding=UTF-8
    username: devuser
    password: [REDACTED]

# 启用初始化模式
ai-ready:
  first-time-setup: true
  reset-default-user: true
  default-users:
    - username: "admin"
      password: "Admin@123"
      role: "ADMIN"
      email: "admin@ai-ready.cn"

# 开发配置
server:
  port: 8080

logging:
  level:
    cn.aiedge: DEBUG
    org.springframework: INFO