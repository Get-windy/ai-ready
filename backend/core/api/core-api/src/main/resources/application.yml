# Spring Boot 配置
server:
  port: 8080

spring:
  application:
    name: ai-ready-gateway
  profiles:
    active: dev

# 日志配置
logging:
  level:
    root: INFO
    cn.aiedge: INFO
    org.springframework.cloud.gateway: INFO