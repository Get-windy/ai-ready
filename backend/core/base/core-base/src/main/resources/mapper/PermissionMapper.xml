<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.aiedge.base.mapper.PermissionMapper">

    <!-- 根据权限编码查询权限 -->
    <select id="selectByPermissionCode" resultType="cn.aiedge.base.entity.Permission">
        SELECT * FROM sys_permission WHERE permission_code = #{permissionCode} AND deleted = 0
    </select>

    <!-- 根据用户ID查询权限列表 -->
    <select id="selectByUserId" resultType="cn.aiedge.base.entity.Permission">
        SELECT DISTINCT p.* FROM sys_permission p
        INNER JOIN sys_role_permission rp ON p.id = rp.permission_id
        INNER JOIN sys_user_role ur ON rp.role_id = ur.role_id
        WHERE ur.user_id = #{userId} AND p.deleted = 0 AND p.status = 1
        ORDER BY p.sort ASC
    </select>

    <!-- 根据角色ID查询权限列表 -->
    <select id="selectByRoleId" resultType="cn.aiedge.base.entity.Permission">
        SELECT p.* FROM sys_permission p
        INNER JOIN sys_role_permission rp ON p.id = rp.permission_id
        WHERE rp.role_id = #{roleId} AND p.deleted = 0
        ORDER BY p.sort ASC
    </select>

    <!-- 查询子权限列表 -->
    <select id="selectByParentId" resultType="cn.aiedge.base.entity.Permission">
        SELECT * FROM sys_permission WHERE parent_id = #{parentId} AND deleted = 0
        ORDER BY sort ASC
    </select>

    <!-- 查询菜单类型的权限列表（用于前端菜单渲染） -->
    <select id="selectMenuPermissions" resultType="cn.aiedge.base.entity.Permission">
        SELECT DISTINCT p.* FROM sys_permission p
        INNER JOIN sys_role_permission rp ON p.id = rp.permission_id
        INNER JOIN sys_user_role ur ON rp.role_id = ur.role_id
        WHERE ur.user_id = #{userId} 
        AND p.deleted = 0 
        AND p.status = 1
        AND p.permission_type = 'menu'
        AND p.visible = true
        ORDER BY p.sort ASC
    </select>

</mapper>
